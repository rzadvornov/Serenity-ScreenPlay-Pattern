name: Serenity BDD Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      MAVEN_IMAGE: maven:3.9.4-eclipse-temurin-21
      APP_DIR: /app
      BASE_URL: https://waarkoop-server.herokuapp.com/

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Docker image
      run: |
        docker build \
          --build-arg MAVEN_IMAGE=${{ env.MAVEN_IMAGE }} \
          --build-arg APP_DIR=${{ env.APP_DIR }} \
          --build-arg BASE_URL=${{ env.BASE_URL }} \
          -t serenity-tests .

    - name: Run tests and generate Serenity reports inside container
      run: |
        CONTAINER_ID=$(docker run -d serenity-tests tail -f /dev/null)

        # Run tests
        docker exec $CONTAINER_ID bash -c "cd $APP_DIR && mvn clean verify -Drestapi.baseurl=${BASE_URL}" || true

        # Generate reports inside the same container
        docker exec $CONTAINER_ID bash -c "cd $APP_DIR && mvn serenity:aggregate" || true

        # Copy reports back to GitHub workspace
        docker cp $CONTAINER_ID:${APP_DIR}/target ./target

        docker stop $CONTAINER_ID
        docker rm $CONTAINER_ID

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: serenity-reports
        path: |
          target/site/serenity/
          target/failsafe-reports/
        retention-days: 30

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: always()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./target/site/serenity
        destination_dir: serenity-reports
        force_orphan: true

    - name: Comment PR with report link
      uses: actions/github-script@v7
      if: github.event_name == 'pull_request' && always()
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `## 📊 Serenity Test Reports

            The test reports are available at: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/serenity-reports/

            - 📈 [Full Report](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/serenity-reports/index.html)
            - 🎯 [Requirements Coverage](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/serenity-reports/requirements.html)
            - 🧪 [Test Results](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/serenity-reports/capabilities.html)`
          })
